# 기여 가이드 (Contributing)

이 문서는 plato-mcp에 기여하려는 분을 위한 안내입니다. 부산대 공식이 아닌 1인 개발
프로젝트이고, 실제 PLATO(학교 LMS) 계정을 다루는 만큼, 일반적인 오픈소스 기여
절차 위에 이 프로젝트 특유의 규칙(자격증명 취급, 프로젝트 보드 운영 방식)이 얹혀
있습니다. 아래 내용은 이 저장소가 지금까지 실제로 지켜온 방식을 정리한 것이며,
다른 유명 오픈소스 프로젝트(VS Code, Node.js, Kubernetes, Next.js, MCP 공식
servers 레포 등)의 관행을 참고해 보완했습니다.

## 0. 기여 범위 — 뭘 받고, 뭘 안 받는가

기여를 시작하기 전에 먼저 확인하세요. 리뷰 리소스가 메인테이너 1인이라, 범위가
안 맞는 PR은 되돌려 보낼 수밖에 없습니다.

| 분류 | 예시 |
|---|---|
| **환영** | 버그 수정, 보안/자격증명 취급 개선, 기존 도구의 견고성 향상(에러 처리, 로그 마스킹), 문서 오류 수정, 테스트 추가 |
| **환영하지만 먼저 이슈로 논의** | 새 도구 추가(성적/공지/Q&A 외 새로운 PLATO 기능), 기존 도구의 동작 방식 변경, 원격 배포(Smithery) 관련 구조 변경 |
| **잘 받지 않음** | 이 저장소의 핵심 목적과 무관한 대규모 리팩터링, 논의 없이 올라온 대형 기능 PR |

애매하면 PR보다 이슈를 먼저 여세요. 특히 새 도구나 구조 변경은 **이슈 승인 없이
작업부터 시작하지 마세요** — 방향이 다르면 작업이 통째로 버려질 수 있습니다.

## 1. 시작하기 전에

1. [이슈 목록](https://github.com/jin-1119/plato-mcp/issues)에서 같은 문제가
   이미 등록돼 있는지 확인하세요. 없으면 새로 만드세요 — 버그든, 기능이든, 문서
   수정이든 예외 없이 이슈가 먼저입니다.
2. 이 저장소는 [GitHub Project 보드](https://github.com/users/jin-1119/projects/1)를
   작업 상태의 **유일한 기준**으로 씁니다. 라벨이나 이슈 코멘트가 아니라 보드의
   `Status` 필드(Backlog → Ready → In Progress → In Review → Done)를 봅니다.
   외부 기여자가 직접 보드를 조작할 필요는 없고, 이슈에 "작업 시작합니다" 코멘트를
   남기면 메인테이너가 상태를 옮겨 둡니다.
3. 실제 PLATO 계정으로 테스트하게 되는 변경이라면, 본인 계정으로만 테스트하고
   자격증명이 로그/에러 메시지/커밋에 절대 들어가지 않는지 확인하세요
   ([SECURITY.md](SECURITY.md) 참고).

## 2. 이슈가 큰 경우 — 쪼개기

체크리스트 항목이 5개를 넘거나, 서로 관련 없는 코드 영역 3곳 이상을 건드리거나,
한 번의 작업 세션으로 끝나지 않을 것 같으면 작업을 시작하기 전에 이슈를 하위
이슈로 쪼갭니다.

- 부모 이슈에 `## Sub-Issues` 섹션을 두고 하위 이슈 번호를 나열
- 각 하위 이슈는 자기만의 승인 기준(acceptance criteria)을 가짐, "부모 참고"로
  퉁치지 않음
- 하위 이슈 본문에 `Part of #[부모번호]`를 명시
- 라벨: 부모는 `parent`, 하위 이슈는 `sub-issue`

큰 기능이라면 메인테이너가 `[Phase N]` 형태의 상위 추적 이슈로 묶기도 합니다 —
외부 기여자는 보통 이미 쪼개진 하위 이슈 하나를 맡는 형태가 됩니다.

## 3. 브랜치

- `main`에서만 분기합니다. `main`에는 절대 직접 커밋하지 않습니다.
- 브랜치 이름: `<type>/issue-<번호>-<짧은-설명>`
  - `type`: `feature`, `fix`, `docs`, `chore`, `refactor`, `test` 중 하나
  - 예: `feature/issue-73-publish-guide`, `fix/issue-63-password-log-leak`
- 이슈 번호 없이 아주 사소한 수정(오타 등)을 보낼 때는 `fix/<짧은-설명>`처럼
  번호를 생략해도 괜찮습니다 — 다만 애매하면 이슈부터 만드는 쪽을 권합니다.
- 하나의 이슈 = 하나의 브랜치가 기본. 여러 이슈를 한 브랜치에 섞지 마세요.

## 4. 커밋

- 커밋 하나가 논리적으로 하나의 변경만 담도록 하세요(원자적 커밋). "그리고"로
  여러 개를 묶어 설명해야 하는 커밋은 쪼개는 신호입니다.
- 모든 커밋은 테스트가 통과하는 상태여야 합니다 — 중간에 깨진 상태로 커밋하고
  다음 커밋에서 고치지 마세요.
- 커밋 메시지는 짧은 제목 한 줄 + (필요하면) "무엇을, 왜"를 설명하는 본문.
  이 저장소는 `feat:`/`fix:` 같은 Conventional Commits 접두어를 엄격히
  강제하지는 않지만, 아래 형식을 기본으로 합니다:

  ```
  Issue #<번호>: <짧은 설명>

  <필요하면 본문 — 무엇을 왜 바꿨는지>
  ```

- 어차피 머지는 squash 방식이라(아래 참고) 브랜치 안에서의 커밋 개수/메시지
  품질보다 **PR 제목**이 최종적으로 `main`에 남는 커밋 메시지가 됩니다. PR
  제목을 신경 써서 쓰세요.

## 5. 코드 품질 — PR 올리기 전에 로컬에서 확인

CI가 자동으로 검사하지만, PR을 올리기 전에 미리 통과시켜 두세요.

```bash
ruff check .
pytest tests/unit -v
```

둘 다 통과하지 않으면 CI가 막습니다. 새 기능/버그 수정이면 관련 테스트도 함께
추가하세요 — 테스트 없는 동작 변경 PR은 리뷰가 늦어집니다.

## 6. PR 만들기

- 제목: `Issue #<번호>: <무엇을 했는지>` 형식을 권장 (예: `Issue #63: fix PLATO password leaking into uvicorn access log`)
- 본문 구조 (이 저장소가 실제로 쓰는 형식):

  ```markdown
  ## Summary
  이 PR이 무엇을, 왜 하는지 2~3문장.

  ## Changes
  - 변경사항 1
  - 변경사항 2

  ## Related Issues
  Closes #<번호>

  ## Verification
  - [x] pytest / ruff 통과
  - [x] (해당하면) 실제로 확인한 방법 — 예: "본인 PLATO 계정으로 dry_run 확인"
  ```

- `Closes #N`으로 이슈를 반드시 연결하세요 — 머지 시 이슈가 자동으로 닫힙니다.
- 대상 브랜치는 `main`입니다.
- 머지는 **squash 방식만** 씁니다. 브랜치 안의 커밋 개수는 신경 쓰지 않아도
  되지만, 결과적으로 PR 제목 + 본문 요약이 히스토리에 남는다는 점을 기억하세요.
- 머지되면 브랜치는 삭제됩니다.

## 7. 리뷰 & 머지

이 프로젝트는 메인테이너 1인 체제입니다. 대형 프로젝트처럼 승인자 N명, LGTM
합의 같은 절차는 없고, 메인테이너(또는 요청 시 지정된 리뷰어)의 리뷰 + CI 통과
후 머지됩니다. 본인 PR을 스스로 승인/머지하지 마세요.

- CI(`ruff check .`, `pytest tests/unit`)가 반드시 초록불이어야 합니다.
- 이 저장소는 실제 학번/비밀번호/세션 토큰을 다루므로, 자격증명이나 PII가
  로그·에러 메시지·테스트 픽스처에 노출되는지가 일반적인 코드 리뷰보다 더
  꼼꼼히 확인됩니다. 리뷰에서 이 부분이 지적되면 최우선으로 고쳐주세요.
- 리뷰 피드백을 반영한 뒤에는 새 커밋을 푸시하고, 기존 커밋을 억지로 amend/force-push로
  덮어쓰지 않아도 됩니다(어차피 squash됩니다).

## 8. AI 도구 사용에 대해

이 프로젝트 자체가 AI 코딩 에이전트(Claude Code)를 적극 활용해 만들어졌기
때문에, AI 도구 사용 자체를 막지 않습니다. 다만 최근 Node.js, Rust, Home
Assistant 등 주요 오픈소스 프로젝트들이 공통적으로 채택하는 원칙을 그대로
따릅니다:

- AI가 생성한 코드라도 **PR을 올리는 사람이 그 변경을 완전히 이해하고, 직접
  테스트하고, 설명할 수 있어야** 합니다.
- "AI가 짜준 코드라 왜 이렇게 동작하는지 모른다"는 상태의 PR은 리뷰 없이
  반려될 수 있습니다.
- 자격증명 처리, 인증 흐름처럼 민감한 부분을 AI가 생성했다면, PR 설명에 어떻게
  직접 검증했는지(코드 리뷰, 실제 계정으로 테스트 등) 적어주세요.

## 9. 처음 기여하신다면

`good first issue` 라벨이 붙은 이슈부터 보는 걸 추천합니다. 작업할 이슈에
코멘트를 남기고 시작하면(중복 작업 방지), 메인테이너가 프로젝트 보드 상태를
`In Progress`로 옮겨 둡니다. 막히는 부분이 있으면 이슈에 바로 코멘트로
남겨주세요 — 끝까지 붙잡고 있다가 한 번에 질문하지 않아도 됩니다.

## 참고 문서

- [SECURITY.md](SECURITY.md) — 이 서버가 자격증명/세션을 어떻게 다루는지
- [PLAN.md](PLAN.md) — Phase/이슈 구조의 실제 예시
- [프로젝트 보드](https://github.com/users/jin-1119/projects/1)
